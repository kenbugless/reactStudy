let age = 25;
//변수를 선언한다. 
console.log(age); //25가 출력된다.

age =30;
console.log(age) //30

//1. 변수명에는 앞 글자에 기호를 사용할 수 없다. ^, !, + 등
//2. 유일하게 사용할 수 있는 기호는 _와 $는 가능하다. 
//3. 변수명은 숫자가 아닌 문자로 시작해야 한다. 
//4. 예약어를 피해야 한다. 
//5. var를 사용해도 상관은 없음. 
//6. var는 재선언이 가능하다. 변수가 중복되어도 가능하게 된다. 
//변수 중복해서 선언하는 것이 불가능하다. let에 경우에는. 

//7. 상수는 const라는 키워를 통해서 사용한다. 이름으로써 값을 부른다.
//선언 이후에, 값을 절대로 바꿀 수 없다. 

 